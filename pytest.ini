[pytest]
testpaths = tests
# Each case is one app on one guest and takes ~20-60s; -x is almost never what
# you want, because a single bad cell should not hide the other fifty.
addopts = -ra --tb=short
markers =
    behavioural: probes that report but do not fail the run (PAM stack varies)
